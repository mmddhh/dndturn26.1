# DNDTurn：实际游戏链路审查与重构任务书

## 0. 范围与证据

审查版本：用户本次上传的 `DNDTurn-sources(20260925-135634).zip`。主要玩法实现在 `targets/neoforge-26.1`，目标声明为 Minecraft 26.1 / NeoForge 26.1.2.84。纯规则层在 `common`。

本轮完成源码链路检查、原文件行号核对以及规划器调用点扫描；没有修改原始源码，没有运行 Gradle 构建、JUnit、GameTest 或真实客户端。以下“当前实现”是源码事实，不是实机验收通过声明。`AGENTS.md` 引用的 `docs/` 不在该压缩包中，本轮没有假称读过这些文档。当前用户明确提出的玩法要求优先；尚未由用户决定的扣费时机、候选阶段处理等，以下标记为建议。

配套证据：`DNDTurn_gameplay_source_evidence.md`。S01–S16 含原文件路径、原始行号及代码摘录；S16 是对本包全部 92 个 Java 文件的调用点扫描。本文中的源码位置均指本次上传版本。

## 1. 总结

当前玩家链路是：自由观察镜头 → 独立移动许可开关 → 切换角色控制后手动移动 → 独立近战攻击按钮 → 只读战术物品预览。原版放置、破坏、使用和实体交互被门控阻断，但没有相应的目标式战术行动替代。

目标链路应是：俯视战术镜头 → 选择物品或明确的交互能力 → 从世界鼠标命中确定目标 → 服务端解析合法执行位置 → 必要时移动到位 → 执行动作 → 分别结算移动与动作 → 发布结果。

这不是只补两个旋转键、把攻击按钮改名就能修好的问题。现有时间门控、许可、网络去重和观察后结算机制有保留价值；主要需要重构的是输入语义、目标载荷、计划调度、物品行为接入和成本分类。

## 2. 逐段检查

### 2.1 镜头确实是从第一人称位置起步的自由镜头

`client/ClientControl.java:61–64` 在进入会话时设为 CAMERA，并把 `position` 设为玩家眼睛位置，直接复制玩家 yaw/pitch。`mixin/client/TacticalCameraMixin.java:17–23` 将这些值写入主镜头并设为 detached。

因此它并非完全没有虚拟镜头，也不是仍让鼠标直接控制玩家身体，而是独立镜头采用了第一人称初始姿态。缺少围绕角色的焦点、俯视初始偏移、轨道半径和约束俯角。[S02]

### 2.2 WASD 代码存在，但不是要求的地面平移；QE 未实现

`ClientControl.java:130–153,266–281` 收集原版前后左右键作为镜头输入。W/S 使用 `Vec3.directionFromRotation(pitch,yaw)`，所以前后移动包括俯角带来的纵向分量。`cameraUpKey()` 返回 Z，另有 Shift 升降和 Sprint 加速。`ClientControl.java:248–256` 用右键拖动修改 yaw/pitch；滚轮沿 forward 移动镜头，而非改变绕焦点的轨道距离。

`CombatControls.java:24–38,56–68` 没有 QE 旋转绑定。因此准确结论是“WASD 已接线但语义不符，QE 没有实现”。不能仅凭源码断言用户实机上 WASD 一定能正常工作；焦点、事件路由、按键抑制、屏幕接管和镜头更新仍需运行验证。[S03]

### 2.3 鼠标只选战斗成员，不能表达地面与方块目标

`ClientControl.java:204–205` 只在镜头模式的左键释放时调用 `select()`。`295–320` 的射线检测虽然取得 BlockHit，但它仅作为实体选择的遮挡距离。最终只把 encounter 成员的 UUID 写给 `TacticalOverlay.select()`。

地面目的地、方块坐标、命中面、局部命中位置、流体目标、放置朝向均没有成为客户端行动选择的值状态。右键被占为自由镜头转向，不是世界上下文选定。[S04]

### 2.4 移动按钮是身体许可开关，不是 MoveTo

`CombatControls.requestMove():202–224` 发 `MOVE_BEGIN/MOVE_END`，不携带目的地。`ServerCombatService.beginPlayerMove():836–877` 开放玩家移动许可。

但 `ClientControl.blockMovement():119–127` 在 CAMERA 模式仍会阻止角色输入；实际手动行走需要切到 CHARACTER 模式。许可存在不等于角色会沿路径自动移动，也不等于角色应该跟随镜头 WASD。[S06]

### 2.5 攻击是“选定目标后即时近战”，不是“自动打最近敌人”

`CombatControls.java:92–95` 和 `TacticalOverlay.java:147–151` 将选定的 UUID 发送为独立 ATTACK 请求。`CombatIntentHandler.java:101–105` 直接调用 `ServerCombatService.attack()`。

`ServerCombatService.java:1839–1850` 限定 Player/Zombie 近战，并在超出 `inMeleeReach` 时立即拒绝。`1208–1214` 使用格位置近战条件和视线；没有先接近目标的步骤。

后端并非完全不认识武器：`1854–1857` 读取玩家当前主手攻击属性，`1922–1924` 触发命中后的武器行为。但没有在意图中绑定“玩家选了哪件武器及其哪一种能力”，也没有物品选择驱动的战术动作界面。[S08]

另一个受限点：正常 encounter 构建时只纳入搜索到的最近一个精确类型 Zombie（`1478–1497`）。这与攻击函数自动替换目标不是一回事；后续推广支持范围需要明确处理目标发现和加入。[S12]

### 2.6 战术物品栏只是预览，并且 1–5 被旧动作键占用

`TacticalOverlay.java:135–143,234–242` 只创建显示槽并刷新物品模型，没有绑定物品选择动作。HTML 第 18 行明确写着“物品预览 · 战术消耗与武器选择尚未开放”。

同时 `CombatControls.java:30–34` 把 1–5 分给移动、攻击、疾走、回避、撤离；`105–135` 会消费重叠原版绑定。因此需要重新定义战术物品栏与动作快捷键，而不是继续把普通攻击固定在数字 2 上。[S05,S06]

### 2.7 方块放置、破坏和使用不是“尚未扣费”，而是被直接拦截

`ClientGameplayInputMixin.java:15–28` 阻止破坏、物品使用、方块使用、实体交互和攻击的原版预测入口。`TacticalMinecraftInputMixin.java:13–20` 也阻止原版输入。

服务端 `ServerGamePacketListenerMixin.java:90–124` 对受控玩家取消破坏、UseItemOn、UseItem 和实体 Interact。`VanillaInputPolicy.controlled()` 包含战斗成员，而不仅是暂停中的玩家。

与此同时 `CombatNetwork.IntentKind:96–110` 只有原型战斗按钮和管理消息，载荷没有方块/地面/物品目标；`OperationRecord.Kind:10` 没有放置、破坏、使用；`CombatEngine.requiresAction():1331–1333` 只覆盖 ATTACK、DASH、DODGE、DISENGAGE、HELP。

结论：旧交互被关闭，新战术世界行动没有补上。不能单纯放开这些门控，否则是在绕过回合授权，而不是实现目标式交互。[S07,S09,S10]

### 2.8 寻路工具存在，但没有接入正常玩家行动

对整个压缩包扫描 `TacticalPlanner.propose/revalidate` 和 `new MinecraftCellProbe`，调用只出现在 common 测试与 GameTest。正常玩家指令并没有调用这些工具。[S16]

不能把这个结论夸大为整个项目没有寻路：Zombie 已有单独的 Navigation/MoveLease。玩家网络移动与 Mob Navigation 是两种执行路径，不能直接把 Zombie 的驱动移到 ServerPlayer。

### 2.9 两个必须同步修改的调度障碍

**候选阶段障碍。** 正常入口 `beginEncounter()` 调 `engine.beginCandidate()`。common `beginOperation():1027–1029` 在 CANDIDATE 只允许 ATTACK/END_TURN；玩家移动服务只接受 ACTIVE。因而当前候选阶段不能完成“先走近，再进行首次攻击”。这不是镜头修复可以解决的。[S11,S12]

**组合行动障碍。** `CombatEngine.beginOperation():1030` 在已有 pending 操作时禁止新根操作；子操作分支 `992–1024` 只支持受许可 DAMAGE 与反应。现有 ATTACK 在开始时就扣动作（1046）。因此不能先登记 ATTACK 根再随意嵌入 MOVE，也不能在 MOVE 未结束时再发普通 ATTACK 请求，期望现有引擎自动编排。[S11]

## 3. 目标玩法契约

### 3.1 镜头是镜头，角色执行是角色执行

以 `focus + yaw + pitch + distance` 表达镜头 rig。进入时 focus 取受控角色附近，初始化为高处斜俯视；俯角是可调表现参数，不复制玩家抬头低头的状态。

WASD 平移 focus，方向按镜头 yaw 投影到水平面，不沿 pitch 向量飞行。QE 围绕 focus 改 yaw；滚轮调整 distance。镜头更新不修改角色位置、身体朝向、动作资源和物品槽。地形高度跟随、镜头碰撞、遮挡处理单独实现，不让 W/S 的 pitch 分量代替高度策略。

右键交给世界目标选择。镜头拖动如保留，使用中键或明确修饰键，不与右键短按抢语义。新增键位仍需可重绑，并处理原版/其他战术键冲突、焦点丢失、打开菜单、拖动与点击判定、重连和死亡。

保留现有 gesture owner、UI 优先级和按键清理机制，不重写为另一套无生命周期约束的静态输入状态。

### 3.2 以所选物品能力与目标决定行为

| 玩家意图 | 目标 | 世界执行 | 动作费用方向 |
|---|---|---|---|
| 选择武器 | 实体或武器支持的目标 | 接近合法攻击位置后攻击；在射程内可直接执行 | 沿用 1 次动作额度 |
| 选择方块物品并进入放置 | 方块面/合法放置位置 | 接近放置范围，复验放置上下文，执行放置 | 1 次动作 |
| 显式选择破坏行为/工具能力 | 世界方块 | 接近破坏范围，按获准的破坏生命周期推进 | 1 次动作，不按包或 tick 重复收费 |
| 使用物品 | 方块、实体、位置或自身 | 按物品能力执行，必要时先接近 | 1 次动作 |
| 使用世界方块本身 | 门、按钮、拉杆、箱子等 | 右键选定后接近合法交互位置，执行该方块自身行为 | 0 动作；接近仍耗移动 |
| 普通移动 | 可达位置 | 到达合法站立位置 | 消耗移动，不消耗普通动作 |

表中“1 次动作”沿用现有 boolean action 模型，不额外引入多点 AP 制。选择 UI 条目与预览不是世界行动，不立即扣费；涉及真正的装备变更时按现有装备/整理规则处理，不把客户端 UI 选择伪装成服务端已换装。

武器可有多个能力，例如斧子的攻击与对方块使用。选择武器的默认能力是攻击，其他用途显式切换，不把物品永远绑定成只能执行一种操作。能力识别由服务端适配/注册机制承担，不堆砌几件 vanilla 物品的特判作为完整实现。

### 3.3 世界方块免费交互与物品对方块使用必须分开

右键是一种输入，不是计费分类。门的开启、拉杆切换即使修改方块状态，也可以是使用世界方块自身；用锄、斧、桶、打火石等对方块执行物品效果，则是物品使用，应扣动作。手里拿着方块并点击箱子时，还可能有“开箱”与“放置”两个明确候选。

为免费 USE_BLOCK 发出的服务端许可只能覆盖选定方块自身的交互，不能开放一个泛化的 UseItemOn 通路后允许它回退到手持物品放置/使用。应在固定版本执行链中确认方块处理和物品回退的分界。不能在副作用发生后才发现“这次其实该收费”。

空手自身没有适用交互的方块，不能凭一次右键就返回免费成功；也不能把所有 BlockEntity 或所有目标为 BlockPos 的行为都判为免费。

### 3.4 目标值必须是有类型的

网络需要能表达至少以下值，并在服务端复验：

- EntityTarget：稳定实体身份、目标类型/能力约束。
- BlockTarget：维度、方块坐标、命中面、命中点，以及执行所需的方块上下文校验。
- GroundTarget：维度、目标位置和站立/通行上下文。
- Self/无目标：只用于能力明确支持的自身物品使用。

物品引用包含槽位/手别与服务端可验证的堆栈身份或版本；具体指纹应由实现定义，不能只校验“还是同一种 item”，忽略关键组件、耐久或数量变化。

身份来自连接。客户端不能提交可信成本、伤害、最终位置或最终有效性。没有适配的物品/目标返回明确 unsupported，不静默退化成最近目标近战。

## 4. 统一执行计划

建议用一个稳定的根请求身份承载完整计划：

```text
右键选定目标 + 当前物品能力
→ 服务端 Resolve / Validate
→ 必要时预留动作，计算可达执行位置
→ Approach：原版移动链逐 tick 推进并观察移动费用
→ Revalidate：目标、物品、距离、视线、阶段和许可仍然有效
→ Execute：真正发起一次攻击、破坏、放置或使用
→ Observe / Commit：记录效果和资源差量
→ 一个明确终态
```

不是所有行动都需要 Approach：已经满足射程/距离、自身使用等应直接进入执行阶段。移动目标是满足该能力执行条件的可达位置集合，不是敌人占据的那一格；近战、远程、方块放置和方块使用的合法执行位置不同。

现有规划权重不等于移动费用。保留服务端观察实际主动位移的结算方法，不按路径节点数量直接改写移动预算。自动移动应有独立的玩家路径执行适配；在同一时刻只保留一个身体驱动，不能让自动输入、手动输入和服务端强制位移一起重复推进角色，更不能用 teleport/setPos 代替正常行走。

### 4.1 版本、许可与幂等

根请求记录首次提交版本与稳定负载指纹；内部步骤使用当前权威状态和阶段许可推进。不能把每个步骤都与最初 encounter 版本机械比较，因为自己的移动结算会推进版本；也不能为了规避 stale-version 把复验完全关闭。

同 ID 同负载返回已有计划状态/结果，同 ID 不同负载冲突。目标失效不自动换成附近单位。网络重试不会再次移动、攻击、扣除物品或扣动作。死亡、换维度、结束回合、退出、掉线、目标卸载和异常都释放该计划拥有的控制状态。

现有子操作许可语义需要显式扩展或增加受约束的计划步骤调度。不能仅给 MOVE 随手填一个 parentId，就假设 common 会接收它。伤害子操作和反应机制需保留，不能因新增计划而失去授权边界。

### 4.2 费用提交时机：建议，不是假定已有规则

把“注册请求”“预留动作”“实际开始执行”“提交结果”分开。选择、预览和合法性检查不花动作；接近阶段可以预留动作避免重复排队，但尚未执行攻击/使用时取消，不应被当作已经完成动作。

有效攻击真正开始后即消耗动作，未命中或零伤害不退款。已经发生的移动独立结算，接近后无法攻击不能把位移伪装成免费。破坏和持续使用按一次根行为计费，不按 START/STOP 包、每个 tick、每个伤害子事件收费。

放置被保护机制拒绝、物品使用返回未处理、破坏中断等情况需明确区分“尚未执行”与“有效尝试已开始”。规则应定义提交点，并写入测试；未知的副作用结果不能盲目重放或退款。不要用“只要世界改变才收费”统一所有行为，否则未命中攻击会错误变成免费；也不要一收到点击就提前扣掉动作。

### 4.3 候选阶段要作显式设计修订

当前 CANDIDATE 限制与所需开场接近行为不兼容。建议保留候选阶段非敌对语义，但明确给当前操作者提供受预算约束的移动及非敌对世界交互；首次敌对执行按既定规则触发战斗激活与先攻。

首次敌对意图在选择时、接近时还是实际执行时触发激活，是会影响多人暂停、先攻及偷袭的规则决策。实施前核对完整仓库中的规则，记录采用的修订。不能为了通过移动测试直接无条件把所有候选会话置为 ACTIVE，或者暗中开放无预算移动。

## 5. 建议实施阶段

### 阶段 A：镜头、输入与目标值

涉及 `ClientControl`、`CombatControls`、`TacticalCameraMixin`、`TacticalMouseMixin`、`TacticalOverlay` 和 UI 资源。

交付俯视 rig、WASD 水平平移、QE 轨道旋转、可重绑与冲突处理、右键有类型目标选择、明确预览和取消。旧“右键转自由镜头”退出默认主交互。物品 UI 必须能选择能力，而不只是显示图标。此阶段不宣称尚未接入的服务端行为已经能执行。

验收：不同进入前视角都得到可预测的俯视镜头；WASD 不导致角色位移和动作消耗；QE 不改变角色身体朝向；UI 手势不穿透世界；改键、菜单、失焦和重连清理正确。

### 阶段 B：统一计划，贯通移动后攻击

涉及协议、`CombatIntentHandler`、新的服务端计划调度、玩家路径驱动、`CombatEngine` 的受限步骤授权。保留原有身份、epoch、许可、位移观测和伤害证据。

交付同一意图下的 MoveTo 与 ApproachThenExecute。正式串联 planner 与世界 probe，处理执行位置、阶段、版本、取消、物品变更、目标失效和预算不足。解决候选阶段语义冲突。选武器后选远处合法目标，应能自动接近再发起一次攻击。

验收：有路、无路、超预算、目标移动/死亡、取消、重复请求、延迟/乱序、候选阶段开场、角色与远端镜像一致性。

### 阶段 C：方块与物品行为、统一成本

涉及动作分类/成本策略、物品能力解析、固定版本原版适配、预测授权与服务端许可、`ServerPlayerTickMixin` 和持续使用身体门控。

接入放置、破坏、普通物品使用、持续使用，以及独立的免费世界方块交互。不要简单移除所有门控；正常入口只放行计划所需能力。特别审查块交互返回未处理后到物品使用的回退路径。

持续破坏、吃喝、蓄力和投射物必须定义单次根操作的生命周期和子效果归属；只允许开始使用而所有后续身体 tick 仍暂停，不算完成。

未分类行为不能因为 `requiresAction` 的 default=false 静默免费。对不支持的物品/能力报告明确边界，不能宣称一个通用事件覆盖了全部模组实现。

### 阶段 D：真实玩法验收与旧入口收束

扩展 common 测试、GameTest、真实 client + dedicated server 测试。替换仅证明“自由镜头移动了一点”的测试期望；对现有“候选阶段不能移动”断言按明确规则修订，不偷偷删除用例。

1–5 快捷键与武器/物品选择保持一致；独立“移动许可开关、攻击附近目标”不得继续充当正式主交互。兼容保留只能作为显式开发入口。更新规则修订、版本契约、缺口文档与验收记录，不把方法存在或编译通过当作玩家链路完成。

## 6. 端到端验收场景

| 场景 | 必须观察到的结果 |
|---|---|
| 玩家原先水平看、抬头看、低头看后分别进入战术模式 | 初始镜头均为围绕角色的俯视 rig，不复制眼睛姿态 |
| WASD、QE、缩放、打开关闭背包、改键、失焦 | 镜头按预期变化；角色和资源不受镜头输入影响；无遗留输入 |
| 选武器，右键选择需要绕障碍接近的目标 | 选择身份保持不变；沿合法路径接近；到位复验；攻击一次 |
| 目标已在攻击范围 | 不进行多余接近；按该武器能力直接执行 |
| 目标在途中移动、死亡、卸载或离开合法范围 | 有界重规划或明确中断；不自动攻击旁边其他实体 |
| 无路、移动不足、途中取消 | 未执行的后置动作不伪称成功；已经发生的移动如实记录 |
| 选方块物品，右键选择放置面 | 正确目标/方向与服务端堆栈；成功执行一项放置行为并按规则扣动作 |
| 选择破坏一个需要多 tick 的方块 | 授权的破坏推进；一个根操作只收费一次 |
| 对同一世界方块分别“使用其自身”与“用手持物品处理” | 世界方块自身交互为 0 动作；物品使用为付费；无错误回退 |
| 右键远处门、拉杆或箱子且当前动作已用完 | 可在剩余移动能力允许时接近并进行免费交互，不得到免费远距效果 |
| 吃喝、蓄力或其他持续物品使用 | 需要的模拟阶段能推进；不会因身体门控永远卡在开始 |
| 计划中途改变槽位、数量、物品组件或目标方块 | 执行前复验，不能用旧选择冒充新世界状态 |
| 重复同一请求、断线重连、结束回合、退出或死亡 | 无重复副作用；许可/镜头/选择/身体驱动按所有权清理 |
| 两个真实客户端 | 各自镜头独立，角色移动、方块状态、物品与费用以服务端为准 |

## 7. 可直接作为编码任务的要求

以本次上传代码为起点，先在完整开发仓库中读取 AGENTS.md、现行玩家规则与相应固定版本源码。不要沿用“给旧攻击按钮补一个近战移动”这种局部修补作为最终设计。实施以上 A–D 阶段，每阶段交付可验收的完整文件与测试，不输出 patch 作为唯一交付。

共同约束：common 保持 Java 17、无平台依赖；NeoForge 26.1 的平台操作留在 target；不顺带重写无关 loader。网络只传意图与值，费用和效果由服务端裁决；玩家与 Mob 分别适配移动；不以传送冒充行走；不通过放开所有原版输入绕过回合；不以普通 UseItemOn 名称判定免费；不以新枚举的 default=false 判定成本；不让模糊能力静默降级为近战。

每阶段报告实际改动、实际执行过的验证、仍不支持的能力和剩余风险。没有运行客户端时，不能宣称镜头、键位、跨 tick 动作或多人玩法已通过。
