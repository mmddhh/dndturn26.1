# 旧版源码参考

此目录是旧版实现的只读参考，不参与当前 `common + targets/` 构建，也不是开发任务清单。当前阶段、缺口与验收入口见 [M3 acceptance](../02_GAPS_AND_CONFLICTS.md)。

引用旧行为前先查 [有效规则](../03_PLAYER_RULES.md) 与 [架构约束](../../AGENTS.md)。旧版瞬移移动、长期 setNoAi、攻击演出改真实位置、无授权清零无敌帧等不得直接移植。此目录中的旧包名、API、配置与功能存在不能证明当前 target 支持该行为。
