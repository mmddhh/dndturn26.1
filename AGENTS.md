# DNDTurn 程序实现与架构约束

本文件适用于整个仓库，规定长期有效的实现原则、模块边界和架构目标。新增及修改代码按这些约束增量迁移；条目不表示相应能力已全部实现，也不要求为一次改动重写所有系统。游戏数值、键位、阶段进度和临时问题不在此维护；既有实现与目标不符时，记录到缺口文档，不以关闭入口或改写规则掩盖差异。

## 1. 文档职责与工作方式

| 文档 | 职责 |
| --- | --- |
| `AGENTS.md` | 仓库级工程约束及应直接遵循的稳定架构目标 |
| `docs/01_IMPLEMENTED_DECISIONS.md` | 当前源码事实、支持范围及证据；不等于运行验收通过 |
| `docs/02_GAPS_AND_CONFLICTS.md` | 唯一活动缺口、冲突、搁置与待验收登记 |
| `docs/03_PLAYER_RULES.md` | 当前已确认的玩家可观察规则及明确修订；不是实现进度表 |
| `docs/04_ARCHITECTURE_CONTRACTS.md` | 目标职责、接口语义、迁移约束与详细验收合同；不是现成 API 清单 |
| `docs/version-differences/` | 固定版本接入点、支持边界及构建／运行验证依据 |
| `docs/legacy/` | 历史规则来源、决策理由与仍适用的维护约定；不自动覆盖后续明确修订 |

- 开始改动前，阅读本文件和涉及的规则、实现、缺口及架构条目，再检查实际代码与固定版本源码。链接存在不代表已经读取；缺失材料如实说明，不编造其内容。
- `docs/legacy/dndrule.txt` 与 `COMBAT_ARCHITECTURE_DECISIONS.md` 用于追踪来源。明确记录的后续修订覆盖对应旧规定；不能因历史文档被称为“规则来源”就回退当前规则，也不能仅凭文档日期或实现现状决定未说明的冲突。
- 用户当前明确指令优先于仓库约定；执行仍须遵守更高优先级约束。改变已有规则时同步记录具体修订及影响；技术重构不能擅自改变费用、退出、非敌对 Mob 移动或其他已确认玩法。
- 如果存在 `docs/legacy/MAINTENANCE_WORKFLOW.md`，遵循其中仍适用的协作、评审和 CI 约定。相互冲突的内容先指出，不以实现便利暗自选边。
- AGENTS 保留原则，04 解释详细合同，01 维护已实现事实，02 维护未完成项及关闭证据。不要复制另一套阶段进度或验收状态，也不要在规则文档不断追加“本轮通过”。
- 每轮聚焦可验收的纵向改动；描述性组件名称不要求机械创建空壳类。没有改变状态所有权、依赖和生命周期的 facade 拆分，不算完成架构重构。

## 2. 模块与版本边界

| 目录 | 允许内容 |
| --- | --- |
| `common/` | Java 17 兼容的规则、状态、值 DTO、算法、不可变快照和独立测试 |
| `targets/<loader>-<minecraft-version>/` | 对应 target 的入口、注册、事件、Minecraft 适配、Mixin、网络、存档、渲染和 metadata |
| `gradle/target-conventions/` | target 共用构建与发布约定 |

1. common 不引用 `net.minecraft.*`、Forge、NeoForge、Fabric、Mixin、平台网络、平台 Codec 或渲染 API。跨边界使用 UUID、primitive、字符串及自定义值类型。
2. 版本差异由 target 适配器承担；不用运行时版本判断、反射分支或同名 class 覆盖拼接多个 target。一个发布 jar 只对应一个 loader 和 Minecraft 版本，不制作 universal jar。
3. 修改 common 前明确纯值语义及受影响 target。保持其构建兼容，但不借共享接口改动强推尚未批准的功能迁移；未启用能力显式声明，不以静默 no-op 冒充支持。
4. 规则状态不长期持有 Entity、Level、连接、Navigation、Brain 等平台对象；平台对象不能进入持久化 DTO、网络值或结果历史。target 的运行上下文可在有界生命周期内持有引用，但必须核验实例并在结束时释放。
5. 区分网络载荷、服务端规范意图、运行上下文、持久检查点与渲染快照。可复用真正同义的值类型，不把同一个可变对象贯穿所有层，也不因 JSON/Codec 可编码就认定对象可安全恢复。
6. Minecraft API 操作遵循实际侧与线程契约；服务端规则由服务器线程单一写入。异步计算只读取捕获的纯值，回到合法线程后核验当前实例、版本和权限再提交。

## 3. 状态所有者、查询与行动执行

### 3.1 单一权威与明确职责

- Encounter 成员、阶段、先攻、资源、敌对关系、规则版本及操作终态由唯一规则权威裁决。时间 controller、空间索引、网络镜像不维护第二套可裁决成员或资源状态。
- 规则权威不代替 Minecraft 世界事实；世界副作用由平台执行链产生。Lease、区域／调度所有权、实时豁免、历史回执、客户端交互与表现状态各有明确持有者，通过命令、只读快照及明确迁移协作。
- 每个实体最多属于一个有效 Encounter。加入、退出、合并和结束走统一入口，不能靠分散修改多个 Map 维持隐式一致性。
- 区分成员归属域、当前空间／模拟域及效果创建时的因果域。效果跨界不自动改写来源；会话别名只解决身份归一，不自动完成资源、许可和安全范围迁移。
- 提取管理类职责时，明确谁创建、推进、取消、卸载、合并、恢复和销毁状态；不新增竞争同一事实的“修复管理器”。

### 3.2 选择命令、纯查询与三种计划

- 装备选择／整理是显式命令；能力发现、悬停和 AI 评分是无世界副作用的查询。查询不得隐式切槽、修改库存、扣费、移动、启动技能或调用未经审计的 Goal／物品执行函数。
- 可提供明确的“选择并查询”组合命令减少往返，但内部仍先校验并确认选择，再调用纯发现。取消预览不暗中回滚已合法确认的装备变更。
- 数字键和 UI 共用选槽语义，关联槽位、手别、选择修订与服务端确认的物品证据；不靠延迟一 tick 解决时序。物品内容指纹不是天然的实例或代次证明，应与所处容器、槽位和选择版本按需组合。
- 玩家输入和 AI 决策都是意图来源，共用规范化、规则校验、授权、计划与结果后端。AI 不伪造玩家或网络包；通用接口不强制天然能力提供物品槽，也不把所有 `ServerPlayer` 机械换成 `LivingEntity`。
- 回合策略决定下一次做什么；行动计划完成一次选定行为；路径只是到合法执行位置的空间提案。一次攻击结束不天然结束整个回合；执行器报告结果，由回合策略按剩余资源决定后续行动。
- 计划绑定执行者、能力 ID／版本、能力来源、明确目标和必要选择证据。预览不是许可；提交、接近后及必要步骤边界复验。目标失效不能自动改打附近单位；AI 改目标须成为可追踪的新决策或中断。
- 使用满足需求的有序、有界步骤，不为接近后执行引入任意工作流引擎；准备、接近、执行、观察与收尾的职责必须明确。

### 3.3 执行、费用、终态与清理

- 世界副作用前完成可预先确定的参数／结构校验、依赖捕获、资源可用性检查和有限授权；经原版或明确审计的适配入口执行，观察实际效果，再按规则提交差量和不可变结果。
- 费用提交点由玩家规则和行为接受边界决定，不由“收到请求”“开始查询”或“创建计划”统一决定。不得把所有成本强行推迟到最终成功，也不得在接近阶段提前当作已经攻击。
- 规则提交前检查结果结构和不变量，避免参数错误造成部分规则写入；执行前不能伪造尚未发生的最终观察。副作用发生后若处理失败，保留已经确认的证据，不把它当成执行前拒绝。
- 资源预留如采用，只由唯一规则权威管理，并与实际消费／撤销分开；不增加第二套余额或借重构引入新 AP。移动费用、动作费用和物品消耗按各自合同观察，不能按网络包或持续使用每个 tick 重复收动作。
- 世界执行事实、资源提交事实、控制释放结果分别记录，再按明确合同聚合成一个根终态。父计划汇总子效果，不能把父子伤害或费用再相加一次。
- 完成、取消请求、中断和最终释放不同。正常完成也走幂等释放，不以 `cancel()` 含糊承担所有收尾；清理失败不抹去已确认效果，效果未知也不因清理成功变成成功。
- 清理失败时按风险隔离或停止后续调度，并记录控制故障。世界副作用无法确认时保留 `UNKNOWN`，不自动重放或退款；根终态映射须统一并测试，不由不同异常分支随意决定。
- 一个行动只有一个规范终态；旧快照和已发布结果不得回写。后续恢复／纠正使用有来源的追加对账记录，不篡改历史。“原子提交”仅限明确的规则边界，不承诺 Minecraft 世界副作用可回滚。
- 规则配置捕获值或版本，单个执行步骤不能混用变更前后的规则；恢复或配置重载不得静默更换正在执行的能力语义。

## 4. 身份、许可、去重与实时授权

- 区分实体 UUID 与当前实例、连接身份与连接实例、会话 ID 与合并别名、服务器世代与会话序号、规则版本与选择修订、结果游标与演出事件序号。每种身份／版本只回答自己的问题，不混成通用计数器。
- 受本模组授权、执行或拦截的操作应追溯到 operation ID、父／root ID、执行者、来源、目标（若有）、相关快照和会话版本。计划因自身步骤推进而产生的新版本不能被当作外部过期，也不能因此关闭复验。
- operation ID 用于去重，许可用于授权。非空 UUID、同 domain、处于行动窗口或持有一个运行对象，都不是充分授权证明。
- 许可明确当前世代、执行者、所属操作／阶段、能力、作用范围、预算／有效期、使用次数及撤销条件。放行身体 tick 不授权任意输入、攻击或物品行为；ThreadLocal 只桥接当前调用，不能跨 tick、队列、网络或恢复隐式继承权限。
- 重复请求先查规范负载及账本：同 ID 同负载返回已有状态／结果，同 ID 不同负载拒绝。过期检查不得让已完成请求重新执行；规范负载须保留影响行为语义的版本及参数。
- 根行动、反应、被动效果和伤害尝试分开；根操作待决不能阻止合法子效果登记，子效果也不能取得任意新行动权。继承链中同一次伤害不重复登记，真正连锁效果另有子身份；嵌套上下文成对清理并覆盖异常。
- 已合并／结束会话的旧请求仍有明确结局。回执、别名、墓碑与归档策略保留有效重试及因果引用；不能用同一 ID 换义后执行新行为。
- 历史退出回执证明请求已处理，实时暂停豁免证明现在允许什么。实时授权可引用回执来源，但须独立记录生效、成员代次和撤销条件；不能扫描任意旧回执推导当前权限，也不能因历史归档改变当前控制。
- 反应、连锁效果、步骤数及递归均有明确保护上限；达到上限记录稳定原因，不静默丢失已发生的效果。执行许可截止、归档保留期与实体自然寿命不是一回事。
- 诊断开关只改变记录，不改变成员、费用、目标支持和权限，也不创建额外生产会话类型或开发门禁。独立测试夹具可受控构造状态，但不得进入正常世界的授权旁路。

## 5. 原版接入、能力扩展与表现

### 5.1 固定版本源码与接入点

- 以 target 的 `gradle.properties`、实际解析依赖及对应 patched 源码／字节码为依据。资料库近邻版本仅供参照，不能据此升级依赖或宣称当前注入兼容；patched Minecraft 中的 hook 调用也不等于完整 loader 实现源码。
- 新增或修改接入点前，检查完整签名、调用方、执行侧／线程、final／override／super 关系、早退、事件顺序和取消后的维护路径。不能凭旧方法名或基类事件推断所有特殊实体与其他模组均被覆盖。
- 每个关键接入点在版本文档记录类／方法、位置、职责、取消语义、支持范围及运行验证场景。Mixin 编译成功不代表注入成功；升级依赖须复查，不用可选匹配掩盖关键注入失效。
- 优先用满足时序及覆盖要求的原版／loader API，不满足时使用最小范围 Mixin。Mixin 只翻译入口和上下文并调用狭窄接口，不保存全局业务状态或复制规则／费用判断。

### 5.2 移动与空间

- 正常移动经对应实体的控制、tick、travel 和 collision 链。不得循环调用 move／travel 补跑多 tick，不用 teleport／setPos 代替正常行走。
- 玩家网络移动与 Mob Navigation 分别适配权威侧、输入和执行阶段；同一实体同一阶段只有一个身体驱动，不能由手动、自动输入及另一套循环重复推进。
- 原版合法传送、复活、纠正和被动位移保留各自来源与规则，不伪装成付费路径移动。实际位移、执行窗口和资源是不同量，主动／外力混合按已定规则归因。
- 路径快照明确体型、姿态、足迹、移动能力及相关碰撞依赖；执行前和必要边界复验。规划权重与节点数不决定移动费用。
- 终点无碰撞不替代路径边的扫掠、支撑、台阶或跳跃校验；未知／未加载位置不能默认通行，也不能为寻路无界加载世界。
- 区分实体位置、脚底、包围盒中心、方块中心、命中面和站立位置。网格单元不是所有坐标语义的替代；攻击／使用应寻找合法执行位置，不强行走入目标占据处。

### 5.3 时间、区域与环境调度

- 局部 Encounter 不改服务器级 tick rate／freeze 模拟区域暂停。全局模式沿用原版语义并明确玩家、载人实体及局部模式组合；全局冻结不能被局部执行或表现驱动绕过。
- 分别声明世界日程、获准模拟步、行动执行进度、AI 记忆／冷却、监视／归档时钟及客户端装饰时间的暂停和恢复语义。不能把所有计时换成 gameTime、每回合递减或系统墙钟。
- 实体 tick、乘客 rideTick、网络侧玩家身体 tick、输入、客户端预测、despawn 和绝对截止分别审计；取消一个事件不代表完整冻结，停止 AI tick 也不证明其绝对期限已经暂停。
- 区域使用已定连续几何；chunk／section 索引只是优化，不能扩大成整区块暂停。发现采样、参战集合与当前模拟范围保持独立语义。
- 计划方块／流体 tick 暂停时保留条目、剩余延迟、优先级及相对顺序。不能出队后只取消回调，也不能让暂停期间剩余延迟归零。
- 调度接管同时维护查询、去重、区块装卸、保存恢复、合并／退出、区域外任务与原版队列的单一所有权。保留实体／方块实体容器的必要生命周期维护。
- 随机 tick、方块实体、block event、邻居传播、跨界写入、天气及生成分别声明覆盖；暂停计划 tick 不等于完整暂停红石或环境，未知类别不能默认获得自由模拟窗口。
- 环境预算按实际获准推进的阶段结算。未加载、全局冻结或未执行不能伪记为完成；多会话调度须有界并避免单一会话长期占用工作预算。
- 短暂实体有来源、模拟归属、许可和自然结束条件；动作／移动结束不抹去因果。许可失效不等于实体寿命到期，不默认增加模组投射物超时，合并不以存活投射物全部消失为前提。

### 5.4 AI 决策与执行者能力

- 禁止以长期 `setNoAi`、删除 Goal 或全局替换整套 AI 作为默认方案。用有边界的 Lease 分别控制决策、targeting、navigation、move／look／jump control、Brain 及必要子系统。
- 暂停自主决策时保留已授权执行所需导航和控制链；不能取消整个 `serverAiStep` 后期待 Navigation 自动推进。外层 tick、custom AI、运行中的 Goal 与其他任务入口各自审计。
- 原版 Goal／Brain 不天然是纯决策函数。不得先跑一次可能移动、攻击、消耗资源的 AI，再声称只是读取意图；只复用可审计状态与受许可执行路径，无法安全拆分的行为显式适配。
- 决策区分感知、定向关系、目标偏好和能力合法性；服务器能查到实体不等于 AI 已感知。策略只提出行动，不在评分时修改世界；必要决策记忆与世界副作用分开。
- 攻击者技能语义与受害者受伤链不同。调用 hurt 不证明保留了攻击者特殊效果；复用技能入口也不能重复战术命中、伤害或费用。
- “可采样、可暂停、可入场、可成为目标、可移动、可自主决策、可执行具体能力”分轴声明支持。保留已批准的非敌对地面移动，不推断其授权攻击、工作、繁殖或任意原版 AI；未知 Mob 不静默降级为 Zombie。
- 天然、物品和状态授予的能力可以使用不同来源证据。根生物承担回合与资源，命中部件由适配器映射；不为每个 PartEntity 自动分配独立成员身份。
- 尽量门控，减少永久字段修改。必须修改时记录原状态、本模组写入值和 ownership；释放仅恢复仍由自己持有的状态，不覆盖其他模组后续修改。没有字段版本时不虚构可验证恢复，重载后重新取得能力，不反序列化 Goal／Brain／旧路径对象。

### 5.5 世界效果、伤害与交互边界

- 一项能力分别描述稳定 ID／版本、来源、目标合同、规则费用分类、执行驱动及效果类别。不用一个通用 enum 或实体特判同时包办全部语义；费用仍由规则权威裁决，适配器不能自报免费绕过规则。
- 行为声明必要且有界的读取依赖、真实作用范围、执行入口、类型化观察和中断／恢复条件。物品数量／耐久、改块、生成身份、状态施加及伤害分别留证据；不哈希整片世界伪装事务。
- 在首个不可逆副作用前核对当前依赖、加载状态、domain、保护和实际影响位置。点击格不等于放置范围；多格方块、流体和间接传播分别处理。仅有事后事件时，不声称已获得写入前保护。
- 保留已审计精确类型／能力范围；未知 override 或无法安全确定副作用的行为明确拒绝，不宽泛匹配父类宣称支持。直接写入许可不自动授权全部邻居传播、计划更新和后续效果。
- 方块自身交互与对方块使用物品分开。免费交互不能回退为手持物品 use／useOn；打开菜单与菜单中的存取、合成、交易和丢弃分别授权，不借开箱放行任意容器行为。
- 伤害在来源、动态受伤入口和事后观察三个边界关联因果。覆盖 Player／ServerPlayer 等 override 早退，不能只依赖 IncomingDamage 或 Post 事件。
- 未命中、免疫、取消、零伤害、吸收、实际生命损失和死亡分别表达；hurt 返回值不替代观察。战术计算只有一个规则入口，原版桥接只在有效授权上下文生效，不修改全世界伤害。
- 防止已由战术规则替代的护甲、盾牌、附魔等重复减伤；耐久、击退和其他副作用独立审计。战术受伤无敌帧仅在合法入口核验后处理，覆盖延迟命中，不恢复无授权清零或事后改值伪装未发生。
- 投射物销毁／偏转、爆炸伤害／击退／改块／起火、状态和非生命实体销毁分别覆盖；只取消伤害不等于取消全部效果。被拒或未定义效果返回明确原因，不静默成功，也不擅自积累待重放效果。

### 5.6 注册与其他模组适配

- 服务端执行能力与客户端表现能力是两条独立扩展轴；不制作同时包办 AI、移动、渲染、粒子和存档的万能 MobAdapter。客户端显示适配是否安装，不决定服务端行为是否合法。
- 核心提供稳定事实、计划／许可、事件和生命周期；适配器负责自身差异。内置生物和物品也使用同一扩展机制，不持续把物种分支和模型字段塞入核心管理类。
- 注册合同明确 ID、版本、匹配范围、线程、执行或显示通道所有权、依赖和恢复支持。重复 ID 拒绝，多重匹配有确定优先级或冲突诊断；缺失服务端能力拒绝，缺失客户端显示适配保守降级。
- 明确注册／冻结及重载时机。运行计划绑定既定适配版本；重载时保留其所需版本或按明确中断合同处理，不能中途悄悄换实现。
- 同 JVM 模组不是隔离脚本沙箱。许可与兼容保证只针对已审计入口，不宣称能阻止任意其他模组直接改世界；未覆盖路径和平台版本必须明确。

### 5.7 表现、粒子与 CameraRig

- 模拟暂停不等于停止渲染，也不意味着可以为动画恢复完整实体／AI／物品 tick。表现只消费已确认事实与获准事件，不修改真实位置、速度、tickCount、状态期限、伤害或物品数量。
- 持续事实用于重新跟踪／恢复，一次性攻击或受击事件按身份去重；重连不重演全部历史。装饰时钟、权威使用进度和有限演出片段分开，动画关键帧不获得游戏副作用权限。
- 表现运行时负责身份、时间和生命周期；通道适配器持有自身模型状态，声明待机、步态、攻击、使用、受击、特殊动画或视觉发射的所有权。未知 renderer 不被强行覆盖。
- 按客户端更新周期生成可复用的表现快照，渲染只采样；阴影、轮廓、第一人称等多次提取不能重复推进时间。暂停／恢复按通道交接，不线性混合相差很大的绝对年龄造成时间倒退，也不改真实实体时钟补救。
- Render State 是主要入口而非万能末端钩子。检查提取、finalize、派生数据和消费顺序；提前读取的手部／物品状态放固定版本桥接层，不随意修改与已计算阴影等结果冲突的几何字段。
- 粒子引擎更新、粒子发生器和演出信号分别审计；仅恢复纯视觉部分，不额外 tick 整个 ParticleEngine，也不补跑混有状态期限或物品副作用的方法。箱盖等状态信号只按受限展示合同桥接，不放开全部 block event。
- CameraRig 独立持有焦点、姿态、距离、地形约束及碰撞恢复；不修改玩家身体或复用 Mob 表现状态。环境视觉采样限于已加载区域及预算，不能移动真实玩家或强加载区块。具体相机缺陷及验收继续留在缺口文档。

## 6. 网络、客户端状态、持久化与合并

### 6.1 网络协议与交互状态

- C2S 只表达意图，身份来自当前连接。服务端核验实例、世代、成员／owner、阶段、选择、版本、目标、资源和许可；客户端不提交可信伤害、费用或最终位置。
- Wire、common 快照与 target envelope 的版本各自演进，迁移或拒绝范围分别明确，不要求同步自增。旧协议不能恢复已关闭的行为旁路，旧 ID 不能换义执行。
- 解析和输出限制字节、集合数、字符串、枚举、数值有限性、嵌套及路径／日志规模。把处理排入合法线程后仍核验当前连接、实体与权限；字节上限不等于计算量有界。
- 错误使用稳定机器码、明确重试策略和可本地化消息／参数。不得解析 reason 文本决定重发；刷新后重试、查询已有结果、不重试与证据核对分开，`UNKNOWN` 不作为普通网络超时重放。
- S2C 提供有序事实与结果；重复、迟到、缺页、重连和换世界有恢复路径，不每 tick 同步全部历史。测试应用层延迟与重试，不把正常 TCP 顺序描述成任意乱序。
- 客户端交互由一个所有者维护选择、瞄准、查询、菜单、等待确认、执行和取消待确认状态。未提交取消使查询身份失效；已提交取消等待权威终态，不能只清本地字段假装服务器停止。
- 菜单绑定的目标／物品与显示保持一致，迟到响应不能复活取消的预览或提交旧目标。Screen／输入框和同意弹窗优先；手势与按键按接收者消费，避免穿透且不改用户保存的原版键位。
- 连接、实体实例、Encounter 及世代变化时清理过期缓存、选择和投影；失焦、打开屏幕、死亡及异常清合成输入。真实客户端预测与远端实体显示分别验收。

### 6.2 检查点、恢复与历史

- 存档只持久化带 schema 的值、操作／行为版本、捕获配置、待决证据、短暂实体来源、必要 Lease 元数据及结果；平台 Codec 留在 target，旧运行对象和线程上下文不能反序列化复活。
- SavedData 的更新／setDirty 仅表示待保存，不等于立即落盘。会话、实体、区块、物品和 held tick 不是一个原子事务；内存去重不保证任意崩溃下恰好执行一次。
- 恢复先校验候选数据和证据、解析当前实例，再安装可用状态并发布投影；新世代撤销旧许可。按副作用前后、结果前后及不同保存顺序核对，不盲目重新 start 已执行或未知的行为。
- 死亡、换维度、退出、掉线、卸载、关闭、异常及正常完成均有幂等清理；只释放自己拥有的控制，不影响其他主体。需要隔离的未知效果独立于普通行动 Lease 处理。
- 无法确认时记录 `UNKNOWN` 并释放不确定行动控制，但遵守已定安全例外：存活未知箭保留隔离及来源，不因原会话结束就恢复自由推进，不自动销毁、加超时或重放碰撞。恢复不强制加载世界来伪造已核验。
- 结果归档、操作截止与投射物寿命按各自规则执行。有效重试、别名、短暂实体及因果引用延长必要保留；不能用简单 TTL 或清空 Map 破坏去重和当前授权。

### 6.3 合并与所有权迁移

- 合并是跨所有者迁移，不只有规则 Map 合并。先准备／校验衍生值并安全停放相关驱动，再提交规则，按新所有权幂等重绑定区域、别名、调度条目和订阅，最后发布一致投影。
- 按已定安全边界合并并保留资源、资格和历史因果；别名 canonical 化不能替代真实资源迁移，也不能全员重置资源来消除冲突。
- 每一步失败明确已提交事实、可重建的衍生绑定及后续是否安全。可以重做可验证的绑定，不能重做已发生的世界行动；本地布尔标记或 fail-stop 不等于跨崩溃原子性。
- 回执、实时豁免、旧域关闭、许可撤销与投影序号按同一迁移合同对账。恢复协议和运行证据不足时保留明确限制，不声称完整重启续战。

## 7. 文件、资源、依赖与工作预算

| 内容 | 位置与要求 |
| --- | --- |
| 共享 Java／资源 | `common/src/main/java/` 保持 Java 17；`common/src/main/resources/` 只放真正通用资源 |
| target 资源 | 对应 `src/main/resources/`，包含 metadata、Mixin、访问变换及版本专属资源 |
| 测试设施 | common 测试及 target 声明的独立 `gameTest`／客户端测试源集；生产 jar 不混入常开探针 |
| 版本与 loader 参数 | 对应 target 的 `gradle.properties` |
| 共享模组信息 | 根 `gradle.properties` 的 `mod_*` 与 Gradle 运行参数 |
| 本地依赖 | target 的 `libs/`；普通 jar 不自动带传递依赖，不放 sources／javadoc／build 产物 |
| 发布逻辑 | `gradle/target-conventions/publish.gradle`，target 不复制实现 |

- 同路径资源有唯一归属，不依赖覆盖顺序；公开依赖优先正常 Maven 坐标，本地 jar 写明用途、版本及额外依赖。客户端依赖不得污染专服加载路径。
- 不提交 token、账号、密码、私有凭据、IDE 缓存或 build 输出。保留用户无关改动，不以清空存档、重置工作树或删除旧工程完成迁移。
- 迁移顺序为盘点版本／入口／资源、建立 target 边界、保留平台层、抽取纯值、逐 target 验证，再清理旧实现；差异落版本文档，不先删除再恢复功能。
- 按连接／会话／实际服务器 tick 控制查询数、候选、寻路扩展及累计工作量；注册器条目数和单包大小不能替代预算。预览可合并／失效，已接受的执行和费用不能被静默丢弃。
- 实时世界探针只在允许线程使用；缓存依赖选择、物品、体型、区域和必要世界观察，缓存命中不替代执行前复验。完整物品编码／哈希在必要采样点复用，不在每个渲染消费者重复计算。
- 诊断关联操作、阶段、执行者、代次、原因码、费用、效果确定性和 Lease 生命周期；日志故障不改变已确认结果。限制日志体积、敏感数据及高基数指标，性能收益用计数／profiling 证明。

## 8. 构建、验证与完成声明

| Target | Gradle JVM | 编译语言级别 |
| --- | --- | --- |
| `forge-1.20.1` | JDK 21 | Java 17 |
| `fabric-1.20.1` | JDK 21 | Java 17 |
| `neoforge-1.21.1` | JDK 21 | Java 21 |
| `neoforge-26.1` | JDK 25 | Java 25 |

common 保持 Java 17 合约。工具链矩阵随正式迁移更新，并与实际声明／解析结果核验；不混淆 Gradle JVM 与编译语言级别，不在 AGENTS 固定每次变化的协议号、schema 或开发参数。

各 target 为独立 Gradle 根，在对应目录运行 `./gradlew clean build`（Windows 为 `gradlew.bat clean build`）。NeoForge 26.1 的 common 测试为 `./gradlew :common:test`，平台验收为 `./gradlew runGameTestServer`；以实际任务配置核对。根 `-PallTargets=true build` 不替代 NeoForge 26.1 独立构建。

按改动风险分别提供证据：

- **纯值／规则**：状态转移、不变量、非法输入无部分提交、去重、父子结果、费用、取消／释放异常和时间性质。
- **固定版本平台**：GameTest 验证实际 tick、伤害、碰撞、作用范围、门控、早退和反例；测试源集编译或注册成功不等于测试执行。
- **真实输入／联机／显示**：从 KeyMapping、鼠标拾取、菜单开始，经网络到专服，覆盖预测、迟到响应、取消、镜头和双客户端。直接调用 choose／target 或嵌入测试玩家不能替代实际输入链。
- **生命周期／持久化／规模**：实例替换、卸载、合并、正常关服、重启、故障窗口及性能；Codec 往返和内存恢复不替代真实保存及崩溃对账。
- **扩展兼容**：未知适配不误接管；注册后能通过独立兼容测试模块完成差异明显实体的能力／表现闭环，不修改核心调度、表现管理器或通用 Mixin；冲突、版本、取消和卸载均可诊断。
- **构建／资源**：受影响 target 的 metadata、class／resources、版本范围、专服类加载与依赖；测试设施隔离出 main，不恢复常开生产探针。
- **仅文档改动**：检查职责、规则一致性、状态证据、引用和本地链接；无需跑无关构建，也不因此更新功能验收状态。

夹具可用 setPos 布置场景，但不能用传送／无碰撞代替待测行走。随机性可控并记录种子、场地与批次；单次通过不关闭历史间歇问题，删除或迁移测试不等于修复其暴露的缺陷。

不得把测试方法存在、测试编译或历史报告写成本轮通过。报告实际改动、命令、环境、结果、未执行项及剩余范围；工具链／依赖缺失时说明限制，不降低 Mixin 要求、删除失败断言或放宽授权来过关。

保持已明确接通的正常战术入口及其服务端安全校验，不因缺少整体验收重新引入 prototype／实验开关隐藏问题。新增未支持能力仍明确拒绝，开发纵切不自动获得正式支持或发布承诺；只有对应端到端证据充分时才在01／02更新完成状态。已定尚未启用的其他 target 功能范围不因这一约束被自动开放。

发布在对应 target 使用 `publishMods`；需明确发布指令，先 clean build 并检查产物，不从错误 target 发布。项目 ID 可在共享配置，token 只来自环境变量。
