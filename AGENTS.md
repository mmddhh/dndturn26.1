# DNDTurn 程序实现与架构约束

本文件适用于整个仓库，约束程序实现、模块边界、原版接入、状态管理与验证。游戏规则、数值与功能取舍见 `docs/03_PLAYER_RULES.md`；功能缺口与待验收项统一见 `docs/02_GAPS_AND_CONFLICTS.md`；决策理由见 `docs/legacy/COMBAT_ARCHITECTURE_DECISIONS.md`。不要把阶段进度、临时缺口和游戏数值复制到本文件。

## 1. 文档职责与工作方式

- 开始改动前，阅读本文件、涉及的规则条目及其决策依据，再检查实际代码与固定版本源码。
- `docs/legacy/dndrule.txt` 是规则来源；后续决策文档中明确记录的修订按修订执行。发现未说明的冲突时，列出冲突，不以实现便利暗自选边。
- `docs/03_PLAYER_RULES.md` 记录抽象规则与顶层设计；功能缺口、规则疑点及待验收项只在 `docs/02_GAPS_AND_CONFLICTS.md` 维护，不保留已完成内容的进度宣称；固定版本接入契约保留在 `docs/version-differences/`。
- 如果仓库存在 `docs/legacy/MAINTENANCE_WORKFLOW.md`，同时遵循其中适用的协作、评审和 CI 约定。不能把文档链接存在当作文档已经读取。
- 用户当前明确指令优先于仓库约定；据此改变已有规则时，应同步记录具体修订及其影响。
- 规则未定义的部分，不通过实体特判、临时数值或静默降级补成“已定稿”。可以继续完成不受该问题影响的工作。
- 每轮聚焦可验收的改动范围；不为结构整齐而同时重写无关 target，不把可编译等同于功能完成。

## 2. 模块与版本边界

| 目录 | 允许内容 |
| --- | --- |
| `common/` | Java 17 兼容的规则、状态、值 DTO、算法、不可变快照和独立测试 |
| `targets/<loader>-<minecraft-version>/` | 对应 target 的入口、注册、事件、Minecraft 适配、Mixin、网络、存档、渲染和 metadata |
| `gradle/target-conventions/` | target 共用构建与发布约定 |

必须遵守：

1. common 不引用 `net.minecraft.*`、Forge、NeoForge、Fabric、Mixin、平台网络、平台 Codec 或渲染 API。跨边界使用 UUID、primitive、字符串及自定义值类型。
2. 不用运行时版本判断、反射分支或同名 class 覆盖拼接多个 target；版本差异由各 target 的适配器承担。
3. 一个发布 jar 只对应一个 loader 和 Minecraft 版本，不制作 universal jar。
4. 修改 common 前先明确无平台依赖的语义和合约；所有受影响 target 保持可构建。暂未启用功能的 target 显式声明能力，不通过静默 no-op 冒充支持。
5. 规则状态不长期持有 Entity、Level、连接、Navigation、Brain 等平台对象；target 在执行阶段按稳定身份解析它们。不得将平台对象放进持久化 DTO、网络状态或结果历史。
6. Minecraft API 操作在该 API 要求的线程执行。服务端规则状态由服务器线程单一写入；异步任务只处理捕获的值，回到服务端线程后校验版本再提交。

## 3. 状态持有者与执行边界

- Encounter 成员、阶段、先攻、资源与规则版本只能有一个权威持有者。时间 controller、空间索引、网络镜像是投影，不得独立维护第二套可裁决成员状态。
- 每个实体最多属于一个有效 Encounter。加入、释放、合并和结束必须通过统一入口，不能靠修改两个 Map 维持隐式一致性。
- 区分成员 domain、当前空间区域和效果创建时的因果 domain。效果跨过区域边界，不自动改变其来源归属。
- 区分根行动、反应、被动效果和伤害尝试。根行动正在执行，不得阻止其合法子效果登记；子效果也不能借此获得任意新行动权。
- 服务端执行顺序统一为：捕获值快照、验证与授权、通过原版执行、观察实际效果、提交规则差量与不可变结果。
- 一次提交前完成参数校验、成本检查和结果构造。不得先扣费、递增版本或修改状态，再因结果参数无效而抛异常。
- “原子提交”仅指规则状态和结果发布边界，不承诺已发生的 Minecraft 世界副作用可回滚。纠正必须是新的、有来源的操作。
- 跨 tick 行动由有序步骤组成；每步有实际观察时刻、原因和效果，行动只有一个终态。旧快照与已发布结果不得回写。
- 规则配置需要明确版本或捕获值；同一执行步骤不得混用变更前后的配置。

## 4. 操作身份、许可与去重

- 每项受本模组授权、执行或拦截的操作可追溯到 operation ID、父/root ID、owner、来源、目标（若有）、快照和 Encounter 版本。
- 操作 ID 用于去重，执行许可用于授权；非空 UUID、同 domain 或处于行动窗口都不是完整授权证明。
- 许可明确主体、行为种类、作用范围、预算/有效期和撤销条件。放行身体 tick 不等于允许所有输入、攻击和物品操作。
- 重复请求先查结果账本；同 ID 同负载返回已有状态/结果，同 ID 不同负载拒绝为冲突。过期版本检查不能使已完成请求重新执行。
- 不把调用同一伤害方法的继承链登记成多次尝试；连锁伤害等真正的新效果使用子操作身份。嵌套执行上下文须成对清理，并覆盖异常路径。
- 已合并/结束 Encounter 的旧请求仍需有明确结局。结果保留期、旧 ID 映射和清理不能破坏仍有效的重试或因果追溯。
- 反应与连锁效果有明确深度/数量上限；到达上限发布原因，不递归到失控。
- 调试命令与生产行动使用显式会话类型/能力边界。调试授权不能绕过正式战术规则。

## 5. 原版接入与源码验证

### 5.1 版本依据

- 以目标 `gradle.properties`、解析出的依赖和对应 patched 源码/字节码为实施依据。其他近邻版本只能辅助理解。
- 新增或修改接入点前，确认完整方法签名、调用方、执行侧/线程、override/super 关系、早退路径、事件顺序和取消后的行为。
- 不能凭旧版本方法名推断当前入口。Mixin 编译成功不代表注入成功；关键注入不能用可选匹配掩盖失效。
- 每个关键 seam 在技术文档记录版本、类/方法、位置、职责、取消语义及运行验证场景。升级依赖时复查这些 seam。
- 优先使用满足时序和覆盖要求的原版/loader API；事件不能满足要求时使用范围最小的 Mixin。Mixin 只桥接，不承载规则、费用或全局状态。
- 不以通用基类事件声称覆盖所有 override、特殊实体和其他模组。未覆盖路径必须有明确支持边界。

### 5.2 移动与表现

- 正常移动经对应实体的原版控制、tick、travel 和 collision 链执行。不得循环调用 move/travel 补跑多 tick，不以 teleport/setPos 替代 locomotion。
- 玩家网络移动与 Mob Navigation 是不同执行路径，分别适配权威侧、输入和运行阶段；同一实体同一阶段不得由两套驱动重复推进。
- 原版合法传送、复活和位置纠正可保留；必须与付费路径移动区分，记录纠正/强制位移来源。
- 路径只是一份提案。快照明确姿态、足迹、移动能力与相关碰撞信息，执行前和必要步骤边界重新核验。
- 目标格无碰撞不能替代整条边的扫掠、支撑、台阶或跳跃验证；未加载区域不得默认为可通行，也不得为寻路无界加载世界。
- 实际位移、执行窗口时长和移动资源是不同量；计费依据已定稿规则观察实际效果。主动移动、击退、下落、载具和纠正分别归因。
- 镜头、HUD、格子和攻击演出不能修改真实实体位置或服务器权威状态。

### 5.3 时间与区域

- 局部 Encounter 不修改服务器级 tick rate/freeze 来模拟区域暂停。全局模式使用原版语义，并明确玩家、载人实体及与局部模式组合时的行为。
- 实体 tick、乘客 rideTick、网络侧玩家身体 tick、输入、客户端预测、despawn 与绝对时间计时器分别审计；取消一个事件不代表完整冻结。
- 区域查询以已定稿几何语义为准。按 chunk/section 建索引可以作为优化，不能暗中把精确区域扩大成整个区块。
- 计划方块/流体 tick 在暂停时保留条目、剩余延迟、优先级与相对顺序。不得在条目出队后仅取消回调，也不得在暂停期间让所有剩余延迟归零。
- 接管调度时同时维护查询、去重、区块装卸、保存恢复、合并/退出、区域外任务和原版队列的单一所有权。
- 随机 tick、方块实体、block event、邻居更新、跨界方块修改以及相关天气/生成行为分别定义接入范围。不能宣称暂停计划 tick 就完整暂停红石或环境。
- 门控执行时保留必要的实体/方块实体生命周期清理，不取消整个容器维护流程。
- 环境执行预算依据实际获准推进的模拟阶段结算；未加载、全局冻结等导致未执行时不能伪记为已完成。
- 有行动归属的短暂实体有来源、许可、自然结束和调度条件；具体超时政策按规则文档执行，不默认施加模组超时。移动预算耗尽或按下结束回合不能使它成为无主效果；合并遵守已定稿安全边界，不以存活投射物作为等待条件。

### 5.4 AI 接管

- 禁止以长期 setNoAi、删除原版 Goal 或替换整套 AI 作为默认方案。
- 用有边界的 AI Lease 表示本模组控制的能力；区分决策、targeting、navigation、move/look/jump control 与 Brain。
- 暂停自主决策时，保留授权行动所需的导航和控制链；不能取消整个 serverAiStep 后期待 Navigation 继续推进。
- 不假设所有 Mob 都由 GoalSelector 控制；Brain/customServerAiStep 与仍在运行的 Goal 需要各自验证。
- 尽量采用门控，减少永久字段修改。确需修改时记录原状态、本模组写入值和 ownership；释放时仅恢复仍由自己持有的状态，不覆盖其他模组后续更改。
- 平台字段不一定提供版本号；不能把不可验证的“版本恢复”写成兼容保证。重载后重新获取能力，不反序列化 Goal、Brain 或旧路径对象。

### 5.5 伤害与跨域效果

- 在来源、动态受伤入口和事后观察三个边界关联因果。覆盖 Player/ServerPlayer 等 override 的早退，不仅监听 LivingIncomingDamageEvent 或 Post。
- 未命中、免疫、取消、零伤害、吸收、实际生命损失和死亡分别表达；hurtServer 的返回值不能替代实际效果。
- 战术计算只由一个规则入口负责；原版适配严格限定在已授权上下文，不修改所有世界伤害。
- 避免原版护甲、盾牌、附魔等重复执行已由战术规则替代的减伤。减伤值、耐久、击退和其他副作用分别审计，不能在过晚事件里改回数值冒充未发生。
- 战术范围内的受伤无敌帧在合法攻击/受伤入口核验后消除，并控制该次战术伤害不生成新的受伤无敌帧；延迟命中也须覆盖。非法请求不得先修改目标，旧版无授权的逐次攻击清零不可恢复。
- Domain 在不可逆效果前检查。投射物销毁/偏转、爆炸伤害/击退/改块/起火、状态施加和非生命实体销毁各自覆盖，不能只取消伤害。
- 拒绝或尚未定义的效果发布明确原因，不静默成功，也不未经规则授权积累待重放效果。

## 6. 存档、网络和生命周期

- 存档持久化值状态、schemaVersion、操作身份、快照版本、待决证据、短暂实体归属、Lease 元数据和必要结果；平台 Codec 位于 target。
- 使用当前版本 SavedData API。setDirty 不等于落盘，Encounter 数据与实体/区块存档不是一个原子事务；不能宣称内存去重保证任意崩溃下恰好执行一次。
- 恢复先校验数据、重新解析实体、核对证据，再发布恢复结果。无法确认副作用时记录 UNKNOWN 并释放不确定控制权，不盲目重放。
- 死亡、换维度、退出、掉线、实体卸载、服务器关闭、异常及正常结束都有幂等清理路径；一次退出事件不应清掉别人的合法控制状态。
- 重启前的服务器 tick/网络序号须有 epoch 或等价会话边界，不能被新会话误认为有效许可。
- C2S 只表示意图。身份来自连接，服务端验证成员、owner、阶段、版本、额度、目标和许可；客户端不能提交可信伤害或最终资源值。
- S2C 提供权威快照和有序结果；重复、过期、重连、换世界和缺失序列有恢复路径。不要每 tick 同步全部结果历史。
- 客户端缓存、选择、镜头和暂停状态在生命周期结束时清理；真实客户端预测与远端实体显示必须单独验收。

## 7. 文件、资源与依赖

| 内容 | 位置与要求 |
| --- | --- |
| 共享 Java | `common/src/main/java/`，Java 17、无平台依赖 |
| 共享资源 | `common/src/main/resources/`，只放确实跨 target 通用的资源 |
| target 资源 | 对应 `src/main/resources/`，包括 metadata、Mixin 配置、访问变换与版本专属资源 |
| 版本与 loader 参数 | 对应 target 的 `gradle.properties` |
| 共享模组信息 | 根 `gradle.properties` 的 `mod_*` 与 Gradle 运行参数 |
| 本地依赖 | target 的 `libs/`；普通 jar 不自动带传递依赖，不放 sources/javadoc/build 产物 |
| 发布逻辑 | `gradle/target-conventions/publish.gradle`，target 不复制实现 |

同路径资源明确唯一归属，不依赖覆盖顺序。公开依赖优先声明正常 Maven 坐标。本地 jar 的用途、版本和额外依赖须明确。不提交 token、账号、密码、私有凭据、IDE 缓存或 build 输出。

迁移顺序为：盘点版本/入口/资源 → 建立独立 target → 保留平台层 → 抽取值逻辑 → 逐 target 构建和运行验证 → 再清理旧实现。禁止先删除旧工程再恢复功能。差异写入 `docs/version-differences/` 等技术文档。

## 8. 构建、验证与完成声明

| Target | Gradle JVM | 编译语言级别 |
| --- | --- | --- |
| `forge-1.20.1` | JDK 21 | Java 17 |
| `fabric-1.20.1` | JDK 21 | Java 17 |
| `neoforge-1.21.1` | JDK 21 | Java 21 |
| `neoforge-26.1` | JDK 25 | Java 25 |

common 始终使用 Java 17 合约。工具链矩阵随正式版本迁移同步更新；以仓库实际声明核验，不混淆 Gradle JVM 与编译语言级别。

各 target 是独立 Gradle 根，在对应目录运行 `./gradlew clean build`（Windows 用 `gradlew.bat`）。NeoForge 26.1 的 common 测试可运行 `./gradlew :common:test`，服务器接入验收运行 `./gradlew runGameTestServer`。根 `-PallTargets=true build` 不替代 NeoForge 26.1 独立构建。

验证按改动风险选择：

- common：状态转移、规则结果、非法输入无部分提交、去重、步骤顺序与生命周期不变量。
- 原版接入：GameTest 验证实际 tick/伤害/碰撞/存档路径、取消与早退、区域边界和至少一个反例。
- 网络/客户端：真实 client 与 dedicated server 验证预测、重连、镜头和显示；嵌入测试玩家不替代联机验证。
- 构建/资源：验证受影响 target 的 jar metadata、共享 class/resources、版本范围和资源加载。
- 仅文档改动无需跑无关构建；检查规则一致性、状态证据与引用即可。

不得把测试方法存在表述为本轮测试通过，也不得把用户先前报告的结果表述为自己本轮复验。完成说明写明改动、验证、未覆盖范围和剩余问题。公共入口须在相应功能端到端验收完成后开放；调试纵切不自动成为正式功能。

发布在对应 target 使用 `publishMods`，发布前执行该 target 的 clean build 并检查产物。项目 ID 可在共享配置，token 只来自环境变量。不要从错误 target 发布。
